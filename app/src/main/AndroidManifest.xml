<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.countryflags">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CountryFlags">
        <activity android:name=".ShveitcActivity"></activity>
        <activity android:name=".DanActivity" />
        <activity android:name=".TaiActivity" />
        <activity android:name=".MadagaActivity" />
        <activity android:name=".ColumActivity" />
        <activity android:name=".ItActivity" />
        <activity android:name=".PolActivity" />
        <activity android:name=".AvstActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>